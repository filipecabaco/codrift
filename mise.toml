[tools]
rust = "1.95.0"
# Pinned for ex_tauri / Burrito bundling. Burrito downloads a prebuilt ERTS for
# the EXACT OTP version from beam-machine-universal.b-cdn.net. 29.0.2 is
# published there (29.1/29.2 are not yet — they 404), so we lock to it to keep
# production bundles reproducible. No OTP downgrade is required.
erlang = "29.0.2"
zig = "0.15.2"
